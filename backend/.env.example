APP_NAME=ScaleUpp
APP_ENV=development
DEBUG=true
API_PREFIX=/api/v1

# Postgres (docker compose)
DATABASE_URL=postgresql+psycopg2://scaleupp:scaleupp@localhost:5433/scaleupp

# JWT (se usará en auth)
SECRET_KEY=change-me-in-production-scaleupp-dev-secret
ACCESS_TOKEN_EXPIRE_MINUTES=60
